# 2026년 8월 KONEPS 파일럿 데이터셋 수집 및 정밀 실증 감사 보고서

**한국어** | [English](PILOT_2026_08.en.md)

본 문서는 대한민국 조달청 나라장터 공공데이터포털 개방표준서비스(`PubDataOpnStdService` v1.2)를 통해 2026년 8월 1개월분(`2026-08-01` ~ `2026-08-31`)의 입찰공고(`bids`), 계약체결(`contracts`), 개찰결과(`awards`, 4대 업무구분 전수) 데이터를 수집하고, 단일 진실 공급원(`data/processed/audits/pilot_2026_08_metrics.json`)을 기반으로 기간 오염을 배제한 정밀 실증 감사를 수행한 공식 기술 보고서입니다.

> [!NOTE]
> **잠정 통계 오염 정정 안내 (Reconciliation Note)**:
> 이전 초기 보고서에서 9월 1일 스모크 테스트 데이터가 일부 파케이 파일 디렉토리에 혼재되어 8월 통계에 포함되었던 기간 오염(Period Contamination) 문제를 구조적으로 해결했습니다. 본 보고서는 실제 이벤트 일시(`bid_notice_date`, `opening_date`, `contract_date`) 기준 2026-08-01 ~ 2026-08-31 범위 내에 속하는 순수 8월 레코드만을 엄격히 필터링하여 재산출한 공인 수치입니다.

---

## 1. 수집 범위 및 분할 전략 (Target Scope & Window Strategy)

- **대상 기간**: 2026년 8월 1일 ~ 2026년 8월 31일 (총 31일)
- **피드별 수집 윈도우 전략**:
  1. `bids` (`getDataSetOpnStdBidPblancInfo`): 1개 월간 윈도우 (31일)
  2. `contracts` (`getDataSetOpnStdCntrctInfo`): 5개 주간 윈도우 (7일 단위 분할)
  3. `awards` (`getDataSetOpnStdScsbidInfo`): 124개 일간 윈도우 (31일 × 4개 업무구분: 물품, 외자, 공사, 용역)
- **페이징 규격**: OpenAPI v1.2 공식 지원 최대치인 `pageSize=999` 적용

---

## 2. 수집 규모 및 API 트래픽 (Collection Metrics)

| 피드 (Feed) | 수집 윈도우 수 | 원천 행 수 (Raw Rows) | API 호출 수 | 산출물 크기 (Gzip) | 상태 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **bids (입찰공고)** | 1 | 32,895 | 33 | 3.76 MB | 100% 완료 |
| **contracts (계약내역)** | 5 | 115,945 | 119 | 25.34 MB | 100% 완료 |
| **awards (개찰결과)** | 124 | 2,120,108 | 2,213 | 114.82 MB | 100% 완료 |
| **전체 합계** | **130** | **2,268,948** | **2,365** | **~143.92 MB** | **전수 성공 (0 Failures)** |

### 개찰결과 업무구분별 세부 수집량
- **공사 (`construction`, 코드 `3`)**: 31개 윈도우, **1,646,350행** (77.65%), 1,668회 호출
- **물품 (`goods`, 코드 `1`)**: 31개 윈도우, **328,201행** (17.79%), 347회 호출
- **용역 (`service`, 코드 `5`)**: 31개 윈도우, **145,425행** (4.54%), 167회 호출
- **외자 (`foreign`, 코드 `2`)**: 31개 윈도우, **132행** (0.01%), 31회 호출

> [!NOTE]
> **API 쿼터 관측 사실**:
> 구성된 인증키를 통해 단일 세션에서 총 2,365회의 API 요청을 에러 없이 성공적으로 완주했습니다.

---

## 3. 무손실 디듀플리케이션 및 그레인 포렌식 감사 (Deduplication Forensics)

### 3.1 후보 키 진화 및 무손실 그레인 확정
초기 5개 컬럼 후보 키 `["bidNtceNo", "bidNtceOrd", "bidprcCorpBizrno", "opengRank", "dqlfctnRsn"]` 적용 시 복수 물품(Multi-lot) 분할 발주(예: `R26BK01634241`)에서 동일 기업이 물품별로 다른 투찰금액을 제출한 1,067건의 정당한 투찰 행위가 축약되는 현상이 발견되었습니다.

이를 방지하기 위해 후보 키를 무손실 규격으로 보정했습니다:
$$\text{Lossless Grain Key} = [\texttt{bidNtceNo}, \texttt{bidNtceOrd}, \texttt{bidprcCorpBizrno}, \texttt{opengRank}, \texttt{dqlfctnRsn}, \texttt{bidprcAmt}, \texttt{bidprcTm}]$$

### 3.2 중복 제거 및 충돌 분류 결과

| 피드 / 업무구분 | 원천 행 수 (Raw) | 완전 일치 중복 (Exact) | 레거시 키 중복 | 신규 무손실 키 중복 | 보존된 정규 투찰 건수 | 최종 Parquet 행 수 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **bids** | 32,895 | 0 | 0 | 0 | - | **32,895** |
| **contracts** | 115,945 | 0 | 0 | 0 | - | **115,945** |
| **awards_goods** | 328,201 | 2,540 | 4,593 | 3,526 | +1,067 | **324,675** |
| **awards_foreign** | 132 | 9 | 9 | 9 | 0 | **123** |
| **awards_construction** | 1,646,350 | 6,050 | 6,074 | 6,074 | 0 | **1,640,276** |
| **awards_service** | 145,425 | 2,490 | 2,551 | 2,551 | 0 | **142,874** |
| **awards 소계** | **2,120,108** | **11,089** | **13,227** | **12,160** | **+1,067** | **2,107,948** |
| **전체 합계** | **2,268,948** | **11,089** | **13,227** | **12,160** | **+1,067** | **2,256,788** |

### 3.3 비완전 일치 충돌(Non-Exact Collisions) 분류 상세
신규 무손실 키 기준 비완전 일치 충돌 **1,071건** 전수 포렌식 분류 결과 (`docs/metrics/pilot_2026_08.json` 기준):

| 분류 | 건수 | 설명 |
| :--- | :--- | :--- |
| **AWARD_BACKFILL** | **876건** | 낙찰자 정보(`fnlSucsfCorpNm`, `fnlSucsfCorpBizrno`, `fnlSucsfAmt` 등) 개찰 후 사후 백필 |
| **SAME_SUBMISSION_STATUS_UPDATE** | **127건** | 동일 투찰 건의 개찰 결과 상태(`opengRsltDivNm`) 변경 또는 부가 필드 갱신 |
| **PRICE_BACKFILL** | **68건** | 예비가격(`rsrvtnPrce`) 또는 기초금액(`bssAmt`) 개찰 후 사후 백필 |
| **DISTINCT_SUBMISSION** | **0건** | ✅ 무손실 확인: 이 분류에 해당하는 정규 투찰 행위 없음 |
| **DISTINCT_CLASSIFICATION_OR_LOT** | **0건** | ✅ 무손실 확인: 업무구분·분할 차이 인한 정규 행 없음 |
| **UNRESOLVED** | **0건** | ✅ 미분류 이상치 없음 |

레거시 5-컬럼 키는 복수 물품 분할 발주에서 **1,067건의 정당한 투찰 행위**를 축약했으나, 신규 7-컬럼 무손실 키에서는 이를 전수 보존합니다.

---

## 4. 최종 낙찰자 카디널리티 실증 (Winner Cardinality)

개찰결과 피드 내 24,978개 고유 공고·차수에 대한 최종 낙찰자 분포:

1. **선정 낙찰자 플래그 (`is_selected_winner == True`) 분포**:
   - **0 낙찰자**: **8,197건 (32.82%)** — 유찰(무응찰, 예정가격 초과) 또는 개찰 직후 적격심사 진행 중인 공고.
   - **1 낙찰자**: **16,741건 (67.02%)** — 표준 단일 낙찰 공고.
   - **2인 이상 복수 낙찰자**: **40건 (0.16%)** — 다수 품목 분할계약(Multi-item) 또는 공동수급(공동이행방식).
2. **다수 낙찰자 사례 실증 구조**:
   - 공고 `R26BK01649062`: 26개 세부 품목에 대해 3개 기업이 품목별로 각각 낙찰 (26개 낙찰 행).
   - 공고 `R26BK01645359`: 5개 분할 품목에 대해 2개 기업이 각각 수주 (5개 낙찰 행).
3. **모델링 권장 규격**:
   - 낙찰 결과를 무조건 1:1로 가정할 수 없으며, `tenders (1) : award_outcomes (0..N)` 관계형 테이블로 모델링해야 정보 유실이 없습니다.

---

## 5. 피드 간 조인 카디널리티 및 정합성 (Cross-Feed Cardinality)

### 5.1 입찰공고 $\rightarrow$ 개찰결과 (`bids` to `awards`)
- 8월 입찰공고 고유 건수: 32,895건
- 8월 개찰결과 고유 건수: 24,978건
- 양쪽 모두 존재하는 공고: 17,853건 (나머지는 8월 이전 공고의 8월 개찰 또는 8월 공고의 9월 개찰)
- **공고당 투찰사 수 분포**:
  - 전체 평균: **84.39개사** (중앙값 3.0개사, 최대 9,675개사)
  - **공사**: 평균 **240.93개사** (중앙값 57.0개사, 최대 9,675개사)
  - **물품**: 평균 **37.02개사** (중앙값 2.0개사, 최대 6,052개사)
  - **용역**: 평균 **15.37개사** (중앙값 2.0개사, 최대 2,297개사)
  - **외자**: 평균 **1.19개사** (중앙값 1.0개사, 최대 4개사)

### 5.2 입찰공고 $\rightarrow$ 계약내역 (`bids` to `contracts`)
- 8월 전체 계약 체결 건수: 115,945건
- **공고번호 직접 연계 계약**: **40,677건 (35.08%)**
- **공고번호 미연계 계약**: **75,268건 (64.92%)**
- **미연계 계약의 사유별 정밀 분류**:
  - **수의계약 (`contract_method == '수의계약'`)**: **72,418건 (96.21%)** — 나라장터 전자공고를 거치지 않고 자체 조달 규정에 따라 직접 체결된 소액 수의계약.
  - **경쟁계약**: **2,850건 (3.79%)** (제한경쟁 1,693건, 일반경쟁 976건, 지명경쟁 181건) — 외부 시스템 연계 또는 오프라인 공고 후 계약서만 나라장터에 사후 등록된 건.
- **공고 기준 계약 체결 분포** (8월 공고 32,895건 기준):
  - 0건 계약 체결: 23,996건 (익월 체결 예정 또는 유찰)
  - 1건 계약 체결: 8,691건
  - 2건 이상 복수 계약 체결: 208건 (분할 계약)

### 5.3 통합계약번호 고유성 실증
- `unified_contract_no` (`untyCntrctNo`): **115,945건 전수 100.0% 고유**, 결측률 0.0% (중복 0건).

### 5.4 공급업체 및 수요기관 차원 실측
- **공급기업 차원 (`suppliers`)**: 10자리 사업자등록번호 기준 총 **143,842개 고유 기업** 식별.
  - 투찰 기업: 123,777개사
  - 최종 낙찰 기업: 13,376개사
  - 계약 체결 기업: 59,233개사
  - **낙찰사의 계약 일치율**: **85.0%** (11,370개사 일치)
- **공공기관 차원 (`agencies`)**: 7자리 표준 기관코드 기준 총 **14,091개 고유 기관** 식별.

---

## 6. 페이지네이션 무결성 감사 (Pagination Integrity Audit)

수집된 원천 데이터를 바탕으로 검증 가능한 항목과 한계를 투명하게 구분했습니다:

1. **소급 실증 완료 항목 (Retroactively Verified)**:
   - **연속 동일 원천 행 탐지 (Immediately Consecutive Identical Lines)**: **7,901건** (일별 수집 경계에 걸친 유찰 공고 레코드로 확인됨).
   - **원천 행 수 대 매니페스트 기대치 일치 (Raw Row vs Manifest Accounting)**: **100.0% 일치** (2,268,948행 대 2,268,948행 정합).
   - **이벤트 일자 수집 경계 엄격성 (Event Date Boundary Enforcement)**: 2026-08-01 ~ 2026-08-31 전수 준수 확인.

2. **소급 실증 불가능 항목 (Not Retroactively Verifiable from Raw Streams)**:
   - **실제 인접 API 페이지 중복 (True Adjacent Page Overlap)**: 원천 스트림에 페이지 인덱스 경계가 분리 기록되지 않아 소급 분리 불가능.
   - **페이지별 SHA-256 해시 지문 반복 (Per-Page Hash Fingerprints)**: 수집 당시 페이지별 구분 태그가 원천 스트림에 미포함.
   - **첫/끝 레코드 지문 충돌 (First/Last Record Fingerprint Collisions)**: 페이지 단위 첫/끝 레코드 분리 메타데이터 부재.
   - **업스트림 API `totalCount` 실시간 변동 (Mid-Stream Drift)**: 엔벨로프 레벨 헤더 정보가 원천 레코드에 미보존.
   - **페이지 번호 루프 (Page Number Loops)**: 실행 시점 페이징 상태 트래킹 메타데이터 부재.

3. **향후 수집 관측성 보강 체계**:
   - 수집기(`Collector`)에 페이지 영수증(`generate_page_receipt`) 훅을 도입하여 실행 ID(`run_id`), 시도 번호(`attempt_id`), 페이지별 SHA-256 해시, `totalCount` 스냅샷, 첫/끝 레코드 지문을 `data/logs/page_receipts/`에 영구 기록합니다.

---

- **스토리지 용량 및 압축 지표 (Storage Footprint)**:
  - **원천 Gzip 압축 용량 (`data/raw/*.jsonl.gz`)**: **131,322,271 바이트** (125.24 MiB / 131.32 MB)
  - **정규화 Parquet 용량 (`data/processed/*.parquet`)**: **148,156,166 바이트** (141.29 MiB / 148.16 MB)
  - **용량 표기 기준**: 이진 메비바이트(MiB, $2^{20}$ 바이트) 및 십진 메가바이트(MB, $10^6$ 바이트)를 엄격히 구분 표기하며, 정합성의 원천 기준은 바이트(Bytes) 단위입니다.
  - **압축 효율**: 비압축 JSON 기준 약 2.8 GB 분량의 226만 건 원천 데이터가 컬럼형 Parquet(Zstandard)을 통해 효율적으로 정규화 보관됨.

---

## 8. 관계형 큐레이티드 테이블 구축 및 검증 결과 (Curated Relational Tables)

2026년 8월 파일럿 원천 데이터를 기반으로 총 7개의 정규화 관계형 큐레이티드 테이블(`CURATED_SCHEMA_VERSION = "1.0.0"`)을 `data/processed/curated/2026_08/`에 Zstandard 압축 Parquet 형식으로 성공적으로 생성했습니다.

### 8.1 큐레이티드 테이블별 규모 및 키 규격

| 테이블 파일명 | 행 수 (Rows) | 물리 기본 키 (Physical PK) | 컬럼 수 | 압축 크기 (Bytes / MiB) | 비즈니스 그레인 및 역할 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `01_tenders.parquet` | 32,895 | `(bid_notice_no, bid_notice_round)` | 32 | 2,084,037 B (1.99 MiB) | 단일 입찰공고 마스터 (100% 고유) |
| `02_bidder_submissions.parquet` | 2,107,948 | `bid_submission_id` (`BID_<32 hex>`) | 29 | 100,861,029 B (96.19 MiB) | 개별 기업 투찰 기록 (7-컬럼 무손실 그레인 보존) |
| `03_award_outcomes.parquet` | 17,315 | `award_outcome_id` (`AWD_<32 hex>`) | 26 | 2,102,572 B (2.01 MiB) | 최종 낙찰 결과 (선정 낙찰자 전수) |
| `04_contracts.parquet` | 115,945 | `unified_contract_no` | 25 | 10,702,406 B (10.21 MiB) | 정부 통합계약 체결 마스터 (100% 고유) |
| `05_suppliers.parquet` | 143,842 | `supplier_id` (`SUP_<32 hex>`) | 10 | 4,572,613 B (4.36 MiB) | 조달 참여 기업 차원 (`masked_biz_no` 포함) |
| `06_agencies.parquet` | 14,091 | `agency_code` | 7 | 224,823 B (0.21 MiB) | 발주 및 수요기관 7자리 표준 차원 |
| `07_tender_contract_bridge.parquet` | 40,677 | `unified_contract_no` | 7 | 743,635 B (0.71 MiB) | 공고-계약 매핑 브릿지 (연계 계약 전수) |
| **전체 큐레이티드 합계** | **2,472,713** | - | - | **121,291,115 B (115.67 MiB)** | **7개 테이블 전수 무결성 검증 통과** |

### 8.2 수학적 정합성 검증 게이트 (Reconciliation Gates)
- **공고 건수 일치**: `len(tenders) == 32,895` (Pass)
- **투찰 건수 일치**: `len(bidder_submissions) == 2,107,948` (Pass)
- **낙찰 건수 일치**: `len(award_outcomes) == 17,315` (Pass)
- **계약 건수 일치**: `len(contracts) == 115,945` (Pass)
- **연계 브릿지 건수 일치**: `len(tender_contract_bridge) == 40,677` (Pass)
- **미연계 계약 보존 일치**: `len(contracts) - len(bridge) == 75,268` (Pass)
- **공급기업 수 일치**: `len(suppliers) == 143,842` (Pass)
- **기관 수 일치**: `len(agencies) == 14,091` (Pass)
- **모든 기본 키 고유성 및 결측치 0건**: 7개 테이블 전수 결측 0건, 중복 0건 실증 완료 (`all_reconciliation_gates_passed: true`).

### 8.3 낙찰금액 결측(24건) 정밀 포렌식 및 결측치 정책
- **현상**: 최종 선정 낙찰자 17,315건 중 24건(0.14%)에서 낙찰금액(`award_amount_krw`)이 `NULL`로 수집됨.
- **포렌식 분석**:
  - 개찰순위 분포: 1순위 23건, 19순위 1건.
  - 계약 체결 방식: 적격심사(`qualification_review`) 21건, 최저가낙찰 1건, 소액수의 1건, 기타 1건.
  - 원인: 개찰 시점 스냅샷에서 적격심사 점수 산정 및 사후 행정 조율이 진행 중이어서 최종 낙찰금액이 확정 기재되지 않은 합법적 행정 상태임.
- **결측치 처리 정책 (`DO NOT IMPUTE`)**: 본 24건은 결측치 대체(Imputation)를 절대 수행하지 않고 순수 `NULL`로 보존하며, 대리 기본 키 `award_outcome_id`(`AWD_<32 hex>`)를 통해 충돌 없이 안전하게 식별됩니다.

### 8.4 시간적 외래키 정합성 (`tender_in_scope`)
월간 수집 윈도우의 경계 효과(Boundary Effect)로 인해 8월에 개찰되거나 계약 체결된 건 중 일부는 7월 이전에 게시된 공고를 참조합니다:
- `bidder_submissions`: 8월 공고 연계 **1,569,500건 (74.46%)**, 이전 공고 연계 **538,448건 (25.54%)**
- `award_outcomes`: 8월 공고 연계 **12,918건 (74.61%)**, 이전 공고 연계 **4,397건 (25.39%)**
- `tender_contract_bridge`: 8월 공고 연계 **9,138건 (22.46%)**, 이전 공고 연계 **31,539건 (77.54%)**
- 관계형 무결성 보존을 위해 `tender_in_scope: bool` 플래그를 제공하여 당월 공고 조인과 전체 시계열 분석을 모두 완벽히 지원합니다.

### 8.5 프라이버시 및 개인정보 비공개 검증 (Zero PII Guarantee)
- 큐레이티드 테이블 7종 전수에서 원천 사업자등록번호(`bidprcCorpBizrno`, `fnlSucsfCorpBizrno`, `rprsntCorpBizrno`), 대표자 성명, 유무선 전화번호, 이메일, 상세 주소 컬럼이 100% 제거되었음을 실증 검증 완료했습니다.
- 공급기업 식별자는 전용 HMAC 키(`KONEPS_SUPPLIER_HMAC_KEY`)로 가명화된 `supplier_id`와 마스킹된 사업자번호(`123-45-*****`)만을 제공합니다.
